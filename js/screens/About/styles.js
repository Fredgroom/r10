import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        marginTop: 20,
    },
    paragraph: {
        fontSize: 20,
        textAlign: 'left',
        margin: 10,
    },
    heading: {
        fontSize: 24,
        fontWeight: 'bold',
        margin: 10,
    }
});